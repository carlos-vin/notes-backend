generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Note {
  id        Int      @id @default(autoincrement())
  titulo    String
  texto     String
  createdAt DateTime @default(now())
}